--- pages/Indicators/IndicatorsList.tsx
+++ pages/Indicators/IndicatorsList.tsx
@@ -285,7 +285,7 @@
                 return (
                   <button
                     key={indicator.id}
-                    onClick={() => navigate(`/indicators/${indicator.id}`)}
+                    onClick={() => toast.info('Détails disponibles bientôt')}
                     className={`group rounded-2xl border border-gray-200 bg-white p-5 text-left shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg ${tone.border} border-l-4`}
                   >
